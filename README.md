# nvim-onedark

My implementation of the onedark theme in Neovim. It is mainly intended for my own use so it
deliberately lacks features and configuration options.

## Usage
In your *init.lua*:
```lua
vim.cmd('colorscheme onedark')
```
